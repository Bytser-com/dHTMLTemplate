apiVersion: v1
kind: Service
metadata:
  name: example-web
spec:
  type: LoadBalancer
  selector:
    app: example-web
  loadBalancerIP: 192.168.10.50   # must be in MetalLB IP pool and unused
  ports:
    - name: http
      port: 80
      targetPort: 80
